## What changed?


## Why?


## Release context

<!--
Add links or notes for frontend, plugins, skills, ZenML docs, website, examples, or other indirect work that must happen with or after a release. CI derives directly changed core and plugin release units from the diff. Add `requires:*` labels for follow-up work that the Kitaru diff cannot show. When publication of a directly changed unit is intentionally deferred past the next applicable release, add its exact `release-label` from `release/release-units.toml` and state the intended release timing.
-->


## Reviewer Notes

<!--
Write this as a short guided walkthrough for the reviewer, not a file list.
Explain where the important behavior lives, what is tricky or risky, and what
would break if this change is wrong. Mention files only when you also explain
why that file matters.
-->


## Reproduction

<!--
Give the reviewer a concrete way to see the feature or bug fix end to end.
Prefer a runnable `examples/...` script, CLI flow, or UI path, then say exactly
what they should observe: artifact/checkpoint names, UI state, CLI output,
logs, etc. Do not replace this with only `just check` / `just test`.
-->


## Local checks run

<!-- Optional: list targeted tests/checks you actually ran. Keep this secondary
to the reproduction path above. -->
