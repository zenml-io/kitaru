Kitaru is available through service {{ include "kitaru.fullname" . }} on port {{ .Values.service.port }}.

To connect locally:

  kubectl --namespace {{ .Release.Namespace }} port-forward service/{{ include "kitaru.fullname" . }} 8000:{{ .Values.service.port }}
  kitaru login http://localhost:8000
