- `POST /api/v1/imports` starts an ephemeral worker for the import's job when `KITARU_SERVER_EPHEMERAL_WORKER__BACKEND` is set and no live worker covers the job's tasks. The worker claims only the job's tasks of built-in plugins that match the configured selectors and starts after the response is sent.
