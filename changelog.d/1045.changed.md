- Analyzer configurations accept a positive `min_sessions` override. Both built-in post-import insight analyzers default to five eligible sessions; other analyzers default to one. Imports and analysis reruns below the minimum record a completed task with an explicit skipped result, eligible count, and threshold, without launching analysis. An empty rerun now returns a completed job with that skipped result instead of HTTP 409.
