KITARU_API_URL=http://localhost:8000

# Optional. The deterministic walkthrough does not require a provider key.
# Set the paid-call approval flag to 1 only after an explicit opt-in.
RETURNS_ALLOW_PAID_MODEL=0
OPENAI_API_KEY=
