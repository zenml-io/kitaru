[project]
name = "kitaru-importer-langsmith"
version = "0.1.0"
description = "LangSmith trace importer for Kitaru."
license = "Apache-2.0"
requires-python = ">=3.11"
dependencies = ["kitaru>=0.21"]

[project.entry-points."kitaru.default_plugins"]
langsmith = "kitaru_importer_langsmith.catalog:get_definitions"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/kitaru_importer_langsmith"]

[tool.uv.sources]
kitaru = { workspace = true }
