[project]
name = "kitaru-importer-braintrust"
version = "0.1.0"
description = "Braintrust trace importer for Kitaru."
license = "Apache-2.0"
requires-python = ">=3.11"
dependencies = ["kitaru>=0.21"]

[project.entry-points."kitaru.default_plugins"]
braintrust = "kitaru_importer_braintrust.catalog:get_definitions"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/kitaru_importer_braintrust"]

[tool.uv.sources]
kitaru = { workspace = true }
