[project]
name = "kitaru-importer-opentelemetry"
version = "0.1.0"
description = "OpenTelemetry trace importer for Kitaru."
license = "Apache-2.0"
requires-python = ">=3.11"
dependencies = ["kitaru>=0.21"]

[project.entry-points."kitaru.default_plugins"]
opentelemetry = "kitaru_importer_opentelemetry.catalog:get_definitions"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/kitaru_importer_opentelemetry"]

[tool.uv.sources]
kitaru = { workspace = true }
