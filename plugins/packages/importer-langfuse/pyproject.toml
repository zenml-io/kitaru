[project]
name = "kitaru-importer-langfuse"
version = "0.1.0"
description = "Langfuse trace importer for Kitaru."
license = "Apache-2.0"
requires-python = ">=3.11"
dependencies = ["kitaru>=0.21"]

[project.entry-points."kitaru.default_plugins"]
langfuse = "kitaru_importer_langfuse.catalog:get_definitions"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/kitaru_importer_langfuse"]

[tool.uv.sources]
kitaru = { workspace = true }
