# Changelog

## Unreleased

- Map `decline_request` to the `reject` action in the example `action_executed` question, so jev no longer fails sessions that correctly declined a request.

## 0.1.0 - 2026-09-21

- Add the `judge` evaluator: ask jev your own yes/no, choice, and score questions about a recorded session and store one evaluation result per question.
