---
title: MCP Server
description: Query and manage Kitaru executions through Model Context Protocol tools
---

Kitaru ships an MCP server so assistants can query and manage executions with
structured tool calls instead of parsing CLI text output.

## Install MCP support

<Tabs items={["uv (recommended)", "pip"]}>
  <Tab value="uv (recommended)">
    ```bash
    uv add kitaru --extra mcp
    ```
  </Tab>
  <Tab value="pip">
    ```bash
    pip install "kitaru[mcp]"
    ```
  </Tab>
</Tabs>

## Start the server

```bash
kitaru-mcp
```

The server uses stdio transport by default.

## Configure in Claude Code

Add this to `.mcp.json` in your project root:

```json
{
  "mcpServers": {
    "kitaru": {
      "command": "kitaru-mcp",
      "args": []
    }
  }
}
```

## Tool set

Execution tools:

- `kitaru_executions_list`
- `kitaru_executions_get`
- `kitaru_executions_latest`
- `kitaru_executions_run`
- `kitaru_executions_cancel`
- `kitaru_executions_input`
- `kitaru_executions_retry`
- `kitaru_executions_replay`

Artifact tools:

- `kitaru_artifacts_list`
- `kitaru_artifacts_get`

Connection tools:

- `kitaru_status`
- `kitaru_stacks_list`

## Example query flow

1. Call `kitaru_executions_list(status="waiting")`
2. Ask the user to confirm an action for a pending wait
3. Call `kitaru_executions_input(exec_id=..., wait=..., value=...)`
4. Re-check state via `kitaru_executions_get(exec_id)`

## Authentication and context

The MCP server reuses the same config/auth context as `kitaru` CLI and SDK.
If you can run `kitaru status`, MCP tools use that same connection.

## Replay behavior

`kitaru_executions_replay` starts a new execution and returns:

- `available: true`
- `operation: "replay"`
- the serialized replayed execution payload

Use `from_` for checkpoint/wait selection, optional `flow_inputs` for flow
parameter overrides, and optional `overrides` for `checkpoint.*` / `wait.*`
overrides.
