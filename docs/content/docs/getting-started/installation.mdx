---
title: Installation
description: Install Kitaru with uv or pip
---

<Tabs items={["uv (recommended)", "pip"]}>
  <Tab value="uv (recommended)">
    ```bash
    uv add kitaru
    ```
  </Tab>
  <Tab value="pip">
    ```bash
    pip install kitaru
    ```
  </Tab>
</Tabs>

## Optional extras

Install MCP server support:

```bash
uv add kitaru --extra mcp
# or: pip install "kitaru[mcp]"
```

Install PydanticAI adapter support:

```bash
uv add kitaru --extra pydantic-ai
# or: pip install "kitaru[pydantic-ai]"
```

## Verify Installation

```bash
kitaru --version
```

```bash
kitaru --help
```

## Requirements

- Python 3.12 or later

## Next Steps

Head to the [Quickstart](/getting-started/quickstart) to explore what's
available, see [Execution management](/getting-started/execution-management)
for lifecycle operations, or open [MCP Server](/agent-integrations/mcp-server)
for assistant-native querying.
