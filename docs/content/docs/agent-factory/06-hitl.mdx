---
title: Stage 6 — Your agents need to ask humans things
description: ask_question, a freeform HITL tool that pauses the flow until an operator answers from any surface
---

**Stage file:** `stage_6_hitl.py`

## The problem

Your agent has just drafted a summary and is one step away from posting it to a live channel. Once it posts, it is posted: the webhook fires, the message is out, and there is no un-sending it. Plenty of steps are like that: a sign-off before something irreversible, a judgment call the model should not make alone, an ambiguous fork where guessing wrong is costly to undo. At those points the agent should stop and ask a person, not pick an answer and hope.

## What goes wrong without it

The obvious version is `input()`: stop and read a line from the keyboard. It works on your laptop right up until anything goes wrong. Look at what `input()` actually does, though. It blocks one specific Python process and waits. The half-finished run, all the work the agent did to reach this point, lives in that process's memory and nowhere else. Now the human takes four hours to answer because they are stuck in meetings. For those four hours a process sits pinned open doing nothing. If the machine reboots, a deploy rolls out, or the pod gets evicted in that window, the run is gone: no record, no way to resume, nothing left for the human to answer. And the answer can only come from that one keyboard, never from a dashboard, a CLI on a server, or a REST call from a web form.

## What this stage adds

Stage 6 adds `ask_question`, a tool the agent calls exactly like any other. The agent's view is deliberately boring: it calls `ask_question("What suffix should I add before publishing?")` and, a moment later, gets a string back. As far as the agent knows, nothing unusual happened.

Underneath, something very different happened. The kitaru pydantic-ai adapter routes that call through [`kitaru.wait()`](/guides/wait-and-resume), which writes a wait record to durable storage and lets the flow stop. Not block: stop. The process can exit, the compute can be freed, and hours can pass. When an answer finally arrives, from a terminal, a dashboard, a CLI, or a REST call, Kitaru loads the run back and the agent's `ask_question(...)` returns that answer as though it had been a slow function all along.

<HitlPauseResumeDiagram />

## One-time setup

Stage 6's skill uses the typed services and mocks from Stages 4 and 5. If you skipped those stages, run the setup script once; it builds the images and creates the `wiki-token` and `webhook-token` secrets:

```bash
bash setup.sh
```

## Run it

<Tabs items={["Interactive", "Non-interactive"]}>
  <Tab value="Interactive">

    Recommended for first run.

    ```bash
    DISABLE_CACHE=1 python stage_6_hitl.py
    ```

    When the agent calls `ask_question`, kitaru's local runtime prompts on the same terminal. Type your answer, hit enter, the flow resumes.

  </Tab>
  <Tab value="Non-interactive">

    The server-style path.

    ```bash
    DISABLE_CACHE=1 python stage_6_hitl.py </dev/null &
    # in another terminal, after `Waiting on ask_question:...` shows up:
    kitaru executions list                                          # find the waiting execution
    kitaru executions input <execution_id> --value '"Verified by ops on call"'
    ```

    That's the same shape on a server: the flow runs remotely, the operator answers via the dashboard / CLI / REST API. The flow resumes from the same point and continues.

  </Tab>
</Tabs>

<Callout type="info">
  On the local stack the kitaru runtime polls for input until a 600s timeout, so the non-interactive path *does* wait for the CLI command above on this stack. If you skip the answer, the flow eventually times out rather than auto-resolving. Behavior on remote stacks is the same shape; the difference is just where the wait record lives.
</Callout>

### Env-var toggles

- `DISABLE_CACHE=1` — same as earlier stages.

## What to look for in the logs

The agent's skill (`skills/with-hitl/default-agent/SKILL.md`) runs this procedure:

1. Look up `lookup_wiki(topic="durability")`, a host-side typed service from [Stage 5](/agent-factory/05-typed-services).
2. Draft a 1–2 sentence summary from the first snippet.
3. Call `ask_question("What suffix should I add before publishing?")`. **The flow pauses here.**
4. Append the operator's answer to the summary.
5. Publish via `publish_summary`. Returns `{message_id, posted_at}`.

So the agent goes: typed-service lookup → human pause → publish, all in one durable flow. The pause is the line to watch for:

```text
Kitaru: Checkpoint `default` started.
…  (skill list/read, exec_service lookup_wiki)
Kitaru: Waiting on `ask_question:1:abc12345` (type=external_input, timeout=600s, poll=5s)
                                              ↑ flow paused here
…  (operator runs `kitaru executions input … --value '"Verified by ops on call"'`)
Kitaru: HTTP Request: POST …   ← agent resumes with the answer
…  (exec_service publish_summary)
Kitaru: Checkpoint `default` finished in 1m12s.

Published 4f12a87bc394 at 1777892841: Durable execution persists every checkpoint output. Verified by ops on call.
```

Once the stage finishes, you can inspect the run from the CLI or the dashboard. From the same shell that ran the stage:

```bash
kitaru executions list                                  # find the execution_id
kitaru executions get <execution_id>                    # full status: turn outputs, wait history, artifacts
kitaru executions logs <execution_id>                   # streaming runtime logs
kitaru executions replay <execution_id> --from default  # re-run from the agent turn (cached HITL answer reused)
```

`replay --from default` re-runs the agent turn with everything before it served from cache. You will not be prompted to answer `ask_question` a second time: the operator's original answer is cached and replayed, because every wait has a stable name that survives the replay. (The "Wait-name design is replay-safe" note under How it works explains the naming.) To swap a checkpoint's input artifact on replay while keeping downstream caches, see [Replay and Overrides](/guides/replay-and-overrides).

## How it works

The pieces involved:

- `agent_factory/tools.py` — adds the `ask_question` tool factory. Body calls `wait_for_input(question=question, name=...)` from the kitaru pydantic-ai adapter. The wait returns whatever the operator supplies; the tool coerces to `str` for the agent's tool-result contract.
- `agent_factory/profile.py` — `ToolName` already includes `ask_question` (forward-looking). Stage 6 just turns it on in `allowed_tools={...}`.
- [`skills/with-hitl/default-agent/SKILL.md`](https://github.com/zenml-io/kitaru/tree/develop/examples/end_to_end/agent_factory/skills/with-hitl/default-agent/SKILL.md) — stage 6's procedure.

A few mechanics worth knowing:

- **No checkpoint plumbing on the example side.** The `wait_for_input` call inside the tool body is the only HITL-specific code in this stage. The adapter handles the suspend/resume mechanics under the hood; the tool body looks like a normal function call that happens to take a while.
- **Waits live at flow scope, and turn mode keeps them there.** `kitaru.wait()` (and the adapter's `wait_for_input` shim) refuses to run inside a checkpoint body; it has to live at flow scope. Stage 6 runs the same teaching setting as the rest of the tour, `granular_checkpoints=False`, so the whole `agent.run_sync()` turn is a single checkpoint and no individual tool call is wrapped in one of its own. That is what lets `ask_question` call `wait_for_input` straight from flow scope, with nothing extra to wire up. (Granular checkpoints change this; the production upgrade path covers what to do.)
- **Wait-name design is replay-safe.** Each wait is identified by a `name` of the form `ask_question:<call_index>:<sha1(question)[:8]>`. The call index makes two same-text questions in one turn distinguishable (the runtime DB enforces a unique constraint on `(run_id, name)`); the question hash makes the name stable across replays as long as the agent emits the same question at the same call index.
- **Schema is `None` (any-JSON), not `str`.** The operator can supply any JSON value (a string, a structured object, etc.). The tool body coerces to `str` for the freeform shape.

## What's simplified for the tutorial

- **The tool calls `wait_for_input(...)` from the body instead of decorating with `@hitl_tool(...)`.** The shipped `@hitl_tool(schema=...)` form persists the `schema` value (a Python `type`) into the wait's metadata, which doesn't round-trip cleanly on the local stack today. Calling `wait_for_input(...)` from inside the tool body has the same external behavior without that snag.
- **Operator input flows downstream verbatim.** Whatever the operator answers becomes the tool's return value, then is appended to the summary, then is POSTed to the webhook. There is no escaping and no re-prompting in the example.

## Production upgrade path

On a server the operator answers through the dashboard, CLI, or REST API rather than the local terminal prompt; the non-interactive run above is already that shape. Before handing operator input to anything that interprets the bytes (an HTML renderer, a shell, SQL), escape it yourself; the example passes it through unescaped.

If you drop the teaching-only `granular_checkpoints=False` and run granular (the upgrade [Stage 1](/agent-factory/01-durable-agent) describes), exempt any wait-bearing tool so it still runs at flow scope: pass `tool_checkpoint_config_by_name={"ask_question": False}` to `KitaruAgent`. That skips the granular checkpoint wrapper for `ask_question` while `exec`, `skill`, and `exec_service` stay granular.

## Source

- [`stage_6_hitl.py`](https://github.com/zenml-io/kitaru/tree/develop/examples/end_to_end/agent_factory/stage_6_hitl.py)
- [Wait, Input, and Resume](/guides/wait-and-resume) — the underlying primitive.

That's the end of the tour. Replay isn't covered as a separate stage here; it's a general Kitaru primitive that applies uniformly to any flow, not a platform-engineering capability specific to agent factories. See [Replay and Overrides](/guides/replay-and-overrides) when you want it.

If you are weighing this pattern for real use, [Production notes and upgrade paths](/agent-factory/production-notes) gathers every teaching stand-in in one place and lists what to harden before you rely on it.
