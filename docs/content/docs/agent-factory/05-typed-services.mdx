---
title: Stage 5 — Your agents need typed services
description: Add exec_service for structured host-side calls (look up a record, create a ticket, publish a summary) when a shell command is the wrong shape
---

**Stage file:** `stage_5_typed_services.py`

## The problem

The agent's `exec` tool is a shell, and a shell is the right tool when the agent is genuinely reasoning about command output: reading a file, grepping a log, checking which version of something is installed. It is the wrong tool the moment the agent wants to do one specific, structured thing: publish a summary, look up a customer record, create a ticket, call an internal control plane. Those actions have a defined input and a defined result, and the result is the point, not the raw bytes that came back.

## What goes wrong without it

With only `exec`, the agent has to hand-assemble the `curl`, get the headers right, and parse the JSON that comes back. That is fragile in both directions: the model can mangle the request (post to the wrong webhook, drop a required field) or misread the response (treat a `500` body as success). A structured action becomes shell-shaped glue the model has to get right character by character, when what the platform wants is typed inputs, typed outputs, and a clear permission boundary.

## What this stage adds

Stage 5 adds a second tool, `exec_service`, for exactly those structured calls. Each service has a typed input and a typed output; the agent picks one by name and fills in its arguments. That splits the work cleanly: `exec` is for shell-shaped work the agent reasons about as command output, and `exec_service` is for structured host-side work where the typed result matters more than the bytes. The agent only sees the services this profile allows, because the tool's description is built from the profile's `allowed_services`.

<ServiceCallPathsDiagram />

## One-time setup

Same `setup.sh` as earlier stages; it also adds the `webhook-token` secret. Idempotent; re-run if you skipped stage 4.

```bash
bash setup.sh
```

## Run it

```bash
DISABLE_CACHE=1 python stage_5_typed_services.py
```

### Env-var toggles

- `DISABLE_CACHE=1` — same as earlier stages.

## What to look for in the logs

The agent follows a skill mixing both paths: sandboxed `exec` for OS info, host-side `exec_service` for the typed lookup + publish:

```text
[mock-services]  Started container … (host=http://localhost:54321)
[proxy]          Started container … (injecting for hosts=['wiki.local'])
[sandbox]        Started container … (proxy-wired)
Kitaru: Checkpoint `default` started.
[sandbox] $ cat /etc/os-release
[sandbox]   → exit=0, stdout=286 chars, cwd=/workspace
[sandbox] $ uname -r
[sandbox]   → exit=0, stdout=17 chars, cwd=/workspace
[mock-services]  GET /snippets/durability (host=localhost:54321, auth=Bearer w…) → 200
[mock-services]  POST /webhooks/team-summaries (host=localhost:54321, auth=Bot we…) → 200
Kitaru: Checkpoint `default` finished in 26.5s.
[sandbox] Stopping container …
[proxy]   Stopping container …
[mock-services] Stopping container …

Published <message_id> at <timestamp>: <one- or two-sentence summary
the agent drafted from the wiki snippet>.
```

The summary text is whatever the LLM produced this turn, so it's not canonical and will vary across runs. The structure is what's stable: `<message_id>` and `<timestamp>` come back as typed fields from `publish_summary`, and the agent stitches them into the line.

Notice what's *not* in the log: there are no `[sandbox]` lines for the `lookup_wiki` or `publish_summary` calls because they run host-side, not in the worker container. The proxy stays idle for those; `[proxy]` only appears at startup/teardown. Only the two `exec` shell commands traverse the sandbox.

## How it works

`exec` and `exec_service` are the two credential paths, and they resolve secrets in different places:

| Path | Where it runs | Creds resolved | Stage |
|---|---|---|---|
| `exec` (sandboxed) | inside the worker container | proxy injects matching `Authorization` headers | 4 |
| `exec_service` (typed) | in the host process | host resolves `kitaru.get_secret(...)` directly | 5 |

The proxy is **not** involved for `exec_service`. The pieces involved:

- [`agent_factory/services/schemas.py`](https://github.com/zenml-io/kitaru/tree/develop/examples/end_to_end/agent_factory/agent_factory/services/schemas.py) — Pydantic models for each service's args + result (`LookupWikiArgs`, `LookupWikiResult`, `PublishSummaryArgs`, `PublishSummaryResult`, `WikiSnippet`).
- [`agent_factory/services/registry.py`](https://github.com/zenml-io/kitaru/tree/develop/examples/end_to_end/agent_factory/agent_factory/services/registry.py) — `ALL_SERVICES` mapping service name → `(args_model, handler, summary)`. `build_service_description(allowed)` renders the dynamic tool description from a set of allowed services.
- `agent_factory/services/lookup_wiki.py` + `publish_summary.py` — the host-side handlers. Each one resolves its credential via `kitaru.get_secret(...)` and makes an HTTP call to the mock.
- `agent_factory/tools.py` — `exec_service` tool factory. Validates `args` against the right Pydantic model on every call, so a malformed arg dict surfaces a Pydantic `ValidationError` to the agent rather than blowing up inside the handler.
- `agent_factory/profile.py` — adds `allowed_services: set[str]` field. The `exec_service` tool's description is built from this set, so the LLM sees only the services this agent can dispatch to.
- `mocks/server.py` — adds `POST /webhooks/{webhook_id}` (Discord-shaped, returns `{message_id, posted_at}`). Auth via `Authorization: Bot webhook-token`.
- `mocks/runner.py` — publishes the mock on a free host port (Docker assigns it) and exports `AGENT_FACTORY_MOCK_BASE_URL` in the host process environment so service handlers can reach it without taking the runner instance as a dependency.
- `skills/with-services/default-agent/SKILL.md` — stage 5's skill. Uses `exec` for the OS-info steps and `exec_service` for the lookup + publish.

A few mechanics worth knowing:

- **Why a flat `(service_name, args)` shape, not a typed union directly?** Some LLM providers handle JSON-Schema `oneOf`/`anyOf` inconsistently. A flat `service_name: str` + `args: dict` parameter shape is reliable across providers; the body re-validates by constructing `<ArgsModel>(**args)` based on the chosen `service_name`. The dynamic tool description embeds each service's args schema as plain text so the LLM sees what it needs to emit.
- **Why host-side, not in the sandbox?** Two reasons. (1) The result is structured, so the agent doesn't need to parse `curl` output; it just gets `WikiSnippet` objects back. (2) Some services (publishing webhooks, hitting internal control planes) shouldn't run from the worker container at all; their credentials never touch the sandbox network. The host-side path keeps that separation explicit.
- **HTTP errors surface to the agent as typed results, not exceptions.** `lookup_wiki` and `publish_summary` catch `urllib.error.HTTPError` and return a result with an error-shaped field (`topic="<error 401: …>"` or `message_id="<error 401: …>"`) so the LLM can reason about the failure rather than crashing the turn.

## What's simplified for the tutorial

The services here hit a local mock (`mocks/server.py`), not real internal APIs, and each handler holds a single credential resolved from a kitaru secret. The shapes are realistic (a wiki lookup, a Discord-style webhook publish), but the endpoints are stand-ins for whatever your platform actually exposes.

## Production upgrade path

Swap the mock handlers for your real internal services, and add per-service authorization where it belongs: the typed boundary is a natural place to enforce which agent may call which action. Adding a service is three files: a new `<name>.py` handler, a new args + result Pydantic pair in `schemas.py`, and a new entry in `ALL_SERVICES`. The tool surface and its dynamic description update automatically. A real platform engineer's skill can mix both paths: `exec` to grep a logfile inside the worker, then `exec_service` to file a typed record in their internal ticketing system.

## Source

- [`stage_5_typed_services.py`](https://github.com/zenml-io/kitaru/tree/develop/examples/end_to_end/agent_factory/stage_5_typed_services.py)
- [`agent_factory/services/`](https://github.com/zenml-io/kitaru/tree/develop/examples/end_to_end/agent_factory/agent_factory/services)

Next up: [Stage 6 — Your agents need to ask humans things](/agent-factory/06-hitl).
