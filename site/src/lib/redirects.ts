/**
 * Configurable redirect targets.
 *
 * Change these constants to repoint redirect routes
 * without touching routing logic.
 */

/** Where /banner should redirect to. Change to repoint later. */
export const BANNER_REDIRECT_TARGET = "/";
